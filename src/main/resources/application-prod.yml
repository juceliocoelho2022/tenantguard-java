spring:
  jpa:
    properties:
      hibernate:
        format_sql: false

management:
  tracing:
    sampling:
      probability: ${OTEL_TRACES_SAMPLER_PROBABILITY:0.1}

app:
  security:
    rate-limit:
      backend: redis
